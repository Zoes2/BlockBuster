<div id="<%= dom_id client %>">
  <tr>
    <th scope="row"><%= client.id %></th>
    <td><%= client.name %></td>
    <td><%= link_to "Mostrar info", client, class: "btn btn-info" %></td>
    <td><%= link_to "Editar", edit_client_path(client), class: "btn btn-warning" %></td>
  </tr>
</div>
